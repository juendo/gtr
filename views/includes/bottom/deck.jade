img.deck(
  ng-src='/images/cards/deck.png',
  ng-alt='Deck',
  ng-click='yourTurn() ? deckClicked(you(), game) : null'
)
.deck-counter
  | {{ game.deck.length }}
button.draw-one(
  ng-click='yourTurn() ? drawOne(you(), game) : null'
)
  | DRAW 1